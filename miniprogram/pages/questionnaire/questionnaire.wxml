<view class="page"><view class="title">问卷填写</view><view wx:for="{{questions}}" wx:key="id" class="question"><view>{{index+1}}. {{item.title}}</view><radio-group><label wx:for="{{item.options}}" wx:key="*this"><radio value="{{item}}"/>{{item}}</label></radio-group></view><button bindtap="submit">提交问卷</button></view>
