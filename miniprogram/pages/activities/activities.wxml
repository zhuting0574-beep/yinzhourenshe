<view class="page"><view class="title">活动报名</view><view wx:if="{{!loading && !items.length}}" class="empty">暂无活动</view><view wx:for="{{items}}" wx:key="id" class="card"><view class="name">{{item.title}}</view><view class="meta">{{item.startTime}} - {{item.endTime}}</view><button size="mini">查看详情</button></view></view>
